# 🦄 مدرسة البنات للإنجليزية

موقع تعليمي للبنات لتعلم الحروف الإنجليزية — منفصل تمامًا عن بوابة الخدمات المساندة،
لكنه يستخدم **نفس مشروع Firebase** (`supportive-med`) للمصادقة وقاعدة البيانات،
مع مجموعات بيانات مستقلة تبدأ بـ `edu` حتى لا تختلط بأي بيانات أخرى.

## ماذا يقدم الموقع؟

| الصفحة | الوصف |
|---|---|
| `/` | تسجيل الدخول (يوزر خاص لكل بنت) + اختيار النشاط |
| `/learn` | كل الحروف كبيرة وصغيرة `A a` مع النطق الصوتي وكلمة مثال بالإيموجي |
| `/flashcards` | استعراض عشوائي للحروف (أحيانًا كبير وأحيانًا صغير) بلا تكرار حتى تكتمل الـ26 |
| `/quiz` | اختبار من 10 أسئلة عشوائية: «استمعي واختاري» أو «طابقي الكبير مع الصغير» — والنتيجة تُحفظ |
| `/admin` | لوحة المدير: إنشاء حسابات البنات، وقت المذاكرة الكلي واليومي، الحروف المستعرضة، ونتائج الاختبارات بالتفصيل (حتى الحروف التي أخطأت فيها كل بنت) |
| `/det` | خطة اختبار Duolingo English Test (للكبار): تقييم صادق للمستوى، استراتيجيات كل نوع سؤال، وقوانين المراقبة — وتربط للصفحتين التاليتين |
| `/det-train` | **المدرّب التفاعلي**: خطة 36 ساعة بجدول متابعة، تمارين موقّتة بمؤقتات الاختبار الحقيقية لكل نوع سؤال (بنوك مولّدة ومفحوصة: 250 كلمة حقيقية/وهمية، 50 جملة فراغات، 8 فقرات مثقوبة، 60 جملة إملاء بالنطق الآلي، 5 قطع تفاعلية، 4 سيناريوهات استماع)، مدرّب كتابة بعدّاد كلمات وإشارات درجة، مدرّب محادثة بتسجيل صوتي وتحويل لنص وحساب كلمة/دقيقة، 10 دروس قواعد، 160 مفردة ببطاقات واختبار، ومحاكاة مصغّرة. **«مراجعتي»**: كل خطأ (كلمة/فراغ/إملاء/مفردة/قاعدة) يُحفظ ويعود بتكرار متباعد (3 صناديق: الآن → 4 ساعات → 20 ساعة)، وزر «راجعها» لأي كلمة، والتمارين تعيد الكلمات الخاطئة تلقائيًا. صفحة «اليوم» تحلّل أضعف مهارة من آخر 3 درجات وتوصي بالتمارين، مع ملخص مستوى ونسخ/استرجاع للبيانات (JSON). **الإصدار 3:** محرك تكيفي يقدّر مستواك لكل نوع سؤال (A2→C1) ويختار الأسئلة على قدّه من مستويات البنوك؛ سجل محاولات كامل (السؤال، إجابتك، وقتك، نص كتابتك وكلامك) يُرفع لحسابك الخاص في Firestore (`detLogs/{uid}`) عبر `det-sync.js`؛ تقييم الكتابة والمحادثة بمعايير ديولينجو (`det-scoring.js`: المهمة/المفردات/القواعد/الهيكل/الشكل مع الأخطاء وتصحيحها) بدل عدّ الكلمات؛ صور حقيقية مرخّصة لتمارين الصور (`assets/det/photos.json`) مع خيار صورة من الجوال؛ محاكاة كاملة بـ14 تمرينًا بترتيب الاختبار بلا تلميحات وتقدير للدرجة؛ صفحة «كل الكلمات» الموحّدة؛ وحزمة المدرّب `det-packs.json` التي يحدّثها كلود بأسئلة وملاحظات مبنية على سجلك. كل شيء يُحفظ في المتصفح (`localStorage` مفتاح `detTrainer.v1`) ويتزامن عند تسجيل الدخول |
| `/study` | **مركز المذاكرة**: مفردات مترجمة و22 موضوعا وقطعا ونماذج كتابة وتحدث وقواعد وقسم اختبارات تفاعلي |

- **النطق الصوتي**: عبر Web Speech API من المتصفح مباشرة — لا يحتاج ملفات صوتية ولا سيرفر (يعمل في كروم وسفاري وإيدج).
- **تتبع وقت المذاكرة**: يُحتسب فقط عندما تكون الصفحة ظاهرة وهناك تفاعل (يتوقف العد بعد دقيقتين بلا لمس)، ويُحدَّث كل 20 ثانية في `eduSessions`.
- **الدخول**: نفس أسلوب البوابة (اسم مستخدم يتحول داخليًا لبريد). حسابات البنات على نطاق مستقل `@edu.app`، وحساب المدير في البوابة يدخل هنا بنفس اليوزر والباسورد (يجري تجريب النطاقين تلقائيًا).

## البيانات في Firestore (منفصلة عن البوابة)

- `eduUsers/{uid}` — حسابات الموقع التعليمي (الاسم، اليوزر، الدور، الرمز، معطّل/نشط)
- `eduSessions/{id}` — جلسات المذاكرة: `uid, activity, date, seconds, letters, startTs, lastTs`
- `eduQuizzes/{id}` — نتائج الاختبارات: `uid, type, correct, total, pct, durationSec, details[]`

## خطوات التشغيل (مرة واحدة)

### 1) قواعد Firestore — تلقائي ✅
قواعد المشروع تُدار من مستودع `supportive`، وقد دُمجت قواعد `edu*`
(الموجودة هنا في [`firestore-learning.rules`](firestore-learning.rules) للمرجعية)
في `firestore.rules` هناك. عند الدفع إلى main في supportive ينشرها
الإجراء التلقائي `firebase-deploy.yml` (يتطلب سرّ `FIREBASE_TOKEN` هناك).
وللنشر اليدوي من مستودع supportive:

```bash
firebase deploy --only firestore:rules --project supportive-med
```

### 2) النشر — تلقائي عبر GitHub Actions ✅
هذا المستودع فيه إجراء نشر تلقائي (`.github/workflows/firebase-deploy.yml`)
يعمل عند كل دفعة إلى `main`: ينشئ موقع الاستضافة الثاني `girls-learning-en`
إن لم يكن موجودًا ثم ينشر الموقع. كل ما يحتاجه:

- أضف سرًّا باسم `FIREBASE_TOKEN` في إعدادات **هذا** المستودع
  (Settings → Secrets and variables → Actions) — نفس قيمة السرّ في مستودع
  supportive (مخرجات `firebase login:ci`).

وللنشر اليدوي من هذا المستودع:

```bash
firebase hosting:sites:create girls-learning-en --project supportive-med   # مرة واحدة
firebase deploy --only hosting --project supportive-med
```

الموقع سيصبح على: `https://girls-learning-en.web.app`

> إن كان اسم الموقع محجوزًا اختر اسمًا آخر وعدّل حقل `"site"` في `firebase.json`
> وفي الـ workflow.

### 2‑ب) صفحة خطة ديولينجو على GitHub Pages — تعمل ✅

صفحة `/det` منشورة أيضًا كموقع مستقل على GitHub Pages:

**https://nadir-coderlab.github.io/Learning/**

الروابط الثلاثة على Pages: الخطة `/Learning/` · المدرّب `/Learning/det-train.html` · مركز المذاكرة `/Learning/study.html`.

الموقع يخدم من فرع **`gh-pages`** (يحوي `index.html` ونسخة باسم `det.html` و`det-train.html` و`study.html` و`det-study.js` و`det-study.css` و`.nojekyll`)، ويتولى
[`.github/workflows/pages.yml`](.github/workflows/pages.yml) مزامنته تلقائيًا من
`public/det.html` و`public/det-train.html` و`public/study.html` وملفات مركز المذاكرة عند كل دفعة إلى `main` — فالتعديل في مكان واحد ينعكس على
Firebase وGitHub Pages معًا. لا تُعدّل فرع `gh-pages` يدويًا.

قراران يفسران التصميم:

- **صفحتا الخطة والمذاكرة لا مجلد `public` كاملًا:** موقع البنات يستخدم مسارات مطلقة
  (`/assets/...` و`/learn`) تنكسر تحت مسار مشروع Pages (`/Learning/`)، بينما صفحة
  الخطة ومركز المذاكرة يستخدمان مسارات نسبية فتعملان من أي مسار. موقع البنات يبقى على Firebase.
- **فرع `gh-pages` لا `actions/deploy-pages`:** التوكن الافتراضي للإجراءات لا يملك
  صلاحية تفعيل خدمة Pages لأول مرة (`Create Pages site → Resource not accessible
  by integration`)، بينما وجود الفرع يجعل GitHub يفعّلها تلقائيًا، والمزامنة
  تحتاج صلاحية الكتابة فقط.

### 3) النطاق في المصادقة — غالبًا غير مطلوب
الدخول هنا باسم مستخدم/كلمة مرور فقط، وهذا لا يتطلب إضافة النطاق إلى
Authorized domains. إن ظهرت مشكلة دخول من النطاق الجديد أضف
`girls-learning-en.web.app` من Firebase Console → **Authentication →
Settings → Authorized domains**.

### 4) أول دخول
- ادخل بحساب المدير (نفس يوزر وباسورد بوابة الخدمات المساندة) — سيُنشأ ملفه تلقائيًا.
- من «📊 لوحة المتابعة» أضف حسابًا لكل بنت (اسم، يوزر، باسورد، رمز 🦄).
- كل بنت تدخل بيوزرها من أي جهاز، وأنت تتابع كل شيء من اللوحة.

## التطوير محليًا

```bash
firebase serve --only hosting
# أو أي خادم ملفات ثابت على مجلد public (مع مراعاة أن cleanUrls تعمل في الاستضافة فقط)
```
