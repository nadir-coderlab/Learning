# مسار 120 — تحديث 2026-09-08

الفرع: `claude/delongo-study-plan-kr15mr`.

## التغيير

- أضيف مسار شخصي في أول صفحة المدرّب. يبدأ المستخدم بنتيجة 85 ودرجات المهارات 75 و80 و90 و100 عبر زر واضح. لا تحفظ هذه النتيجة لجميع الزوار تلقائيا.
- المراحل 95 ثم 110 ثم 120 أهداف تدريب وليست وعودا بنتائج.
- وقت الجلسة 45 أو 60 أو 90 دقيقة يوزع بين المهارات حسب آخر نتيجة رسمية والأداء التدريبي خلال 7 أيام. كل مهارة لها 5 دقائق على الأقل. مجموع الدقائق يساوي الوقت المختار.
- أضيف سجل منفصل للنتائج المعتمدة ونطاقات التجريبي الرسمي. نسب التمارين لا تحول إلى درجات DET. أزيلت المعادلة القديمة من نتائج المحاكاة مع إبقاء السجلات القديمة قابلة للقراءة.
- أضيفت 12 قطعة أصلية و24 سؤالا عن الاستنتاج والدليل. كل إجابة لها دليل حرفي من النص وشرح عربي. الأسئلة تدريب مهاري وليست أسئلة مسربة أو محاكاة رسمية معتمدة.
- ورشة الكتابة تستدعي آخر 12 إجابة مناسبة. تعرض الملاحظات المحفوظة وتتيح إعادة الصياغة ومقارنة المؤشرات النصية مع حفظ النسختين. غياب الإجابات السابقة يفتح مثالا تعليميا واضحا.
- أزيل التوجيه القديم الذي يعد بدرجة في موعد محدد أو يربط النتيجة بمجرد عدد الكلمات من مقدمة المدرّب والخطة.

## الربط والبيانات

- الملفات الجديدة: `public/det-path120.js` و`det-path120.css` و`det-path120-bank.js` و`det-path120-drills.js`.
- الأنشطة الجديدة: `#read/evidence` و`#write/rewrite`.
- تستخدم نفس `detTrainer.v1` ونفس مزامنة `det-sync.js`. البيانات الإضافية في `S.path120` تشمل `records` و`minutes` و`draft`.
- محاولات النشاطين تستخدم `logAttempt` الحالي. أخطاء القراءة تدخل المراجعة المتباعدة. الصياغات المنقحة تحمل `sourceId` و`before` و`checks` والمؤشرات التدريبية.
- لا تعديل على المصادقة أو قواعد Firestore. لا وصول خارجي تلقائي إلى حساب المستخدم. المشاركة تكون عبر ملخص أو تصدير موجود.
- تحديث Pages ينسخ جميع الملفات الجديدة عند النشر المعتاد من `main`. حفظ الفرع وحده لا ينشر الموقع العام.

## التحقق

`node tests/det-path120.test.mjs`

يشمل صلاحية الدرجات وتوزيع الوقت وتغير الأولويات وعزل التجريبي عن المعتمد وإهمال البيانات القديمة في التحليل والتحقق من أدلة القطع وانتهاء مؤقت القراءة وحفظ الصياغة المنقحة وربط ملفات Pages.

فحص JavaScript للملفات الجديدة وللسكربت الداخلي في المدرّب. لم ينفذ اختبار متصفح أو فحص فعلي لحساب Firestore. التقييم النصي الموروث تقريبي ولا يقيس جودة النطق أو يعطي درجة اختبار معتمدة.

## ما يمكن تطويره لاحقا

يمكن توسيع بنك الاستنتاج بعد مراجعة سجل المستخدم. لا ترفع صعوبة جميع الأسئلة إلى C1 لمجرد اختيار هدف 120. أبق الانتقال مبنيا على الأداء. لا تضف تحويلات من نسبة التدريب إلى درجة رسمية دون نموذج صالح ومحقق.
